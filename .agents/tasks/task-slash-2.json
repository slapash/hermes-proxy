{
  "task_id": "task-slash-2",
  "phase": "plugin-slash-commands",
  "plugin": "slash-commands",
  "type": "plugin",
  "spec_version": "1.0",
  "depends_on": ["task-slash-1"],
  "goal": "Polish slash-commands: handle /theme arg, fix edge cases, add keyboard focus management",
  "sub_tasks": [
    "T1: /theme supports optional arg: '/theme light' or '/theme dark' — call HermesProxy.setTheme(arg); if no arg, toggle",
    "T2: After command execution, clear msgInput and hide dropdown",
    "T3: If user types '/xyz' (unknown), show 'No command found' in dropdown or let it send as normal message on Enter",
    "T4: After selecting a command via Enter, refocus msgInput",
    "T5: Click outside dropdown to close it (document click listener with cleanup)"
  ],
  "guard_focus": [
    "Does /theme light correctly set theme?",
    "Does unknown command like '/xyz' allow normal send on Enter?",
    "Is msgInput cleared after executing a command?",
    "Does dropdown hide when clicking outside?",
    "Are document-level click listeners cleaned up on re-init?"
  ],
  "notes": "Adds ~30-40 lines. Builds on task-slash-1."
}
